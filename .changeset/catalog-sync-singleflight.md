---
"executor": patch
---

Single-flight background tool-catalog refreshes by connection across executor stacks sharing a database. Concurrent MCP sessions now join one provider discovery instead of launching duplicate credential prompts and OAuth browser flows.
